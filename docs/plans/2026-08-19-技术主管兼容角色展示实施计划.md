# 技术主管兼容角色展示 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 区分兼容角色 `tech_lead` 与正式角色 `development_lead` 的界面标签，消除重复“开发主管”。

**Architecture:** 仅调整前端角色选项标签。保持内部值和后端权限解析不变，因此历史项目成员及工作流配置继续使用原有 `tech_lead` 键。

**Tech Stack:** Vue 3、Node 展示契约测试。

---

### Task 1: 覆盖兼容角色标签

**Files:**
- Modify: `frontend/src/views/WorkflowView.vue`
- Modify: `frontend/src/views/ProjectDetailView.vue`
- Create: `frontend/src/views/workflowRoleOptionLabels.test.mjs`

**Step 1: Write the failing test**

断言 `development_lead` 显示“开发主管”，`tech_lead` 显示“技术主管（兼容旧配置）”，且不存在第二个同名“开发主管”标签。

**Step 2: Run the test to verify it fails**

Run: `node src/views/workflowRoleOptionLabels.test.mjs`

**Step 3: Write the minimal implementation**

将两个前端角色选项中的 `tech_lead` 标签改为“技术主管（兼容旧配置）”。

**Step 4: Run the test to verify it passes**

Run: `node src/views/workflowRoleOptionLabels.test.mjs`

**Step 5: Run the production build**

Run: `npm run build`
