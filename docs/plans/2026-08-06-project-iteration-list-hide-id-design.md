# 项目迭代列表隐藏 ID 设计

## 目标

移除项目详情页“迭代”标签中面向用户展示的迭代 ID 列，减少无业务含义的编号对迭代名称、状态和交付操作的干扰。

## 方案

仅删除该表格的 `ID` 列。保留迭代实体 ID、详情页路由参数、接口返回字段及所有其他列；需求池继续以“需求池（未排期）”标识，正式迭代名称仍可进入迭代详情。

## 验证

扩展项目详情迭代布局的前端源码契约测试，断言迭代区域不存在 `prop="id"` 的表格列，同时保留需求池标签、迭代名称和操作区的既有断言。执行对应测试和前端生产构建。
