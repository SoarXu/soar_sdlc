# 工作流保存与离开保护设计

## 问题

保存流程图时，未保存离开保护仍可能打开确认对话框。对话框内再次点击“保存流程图并继续”会并发提交第二个保存请求，造成请求竞争和持续加载。

## 决策

工作流设计器只允许一个保存请求在途。`saveGraph` 复用在途 Promise；保存期间，离开保护不打开“放弃未保存修改”对话框并阻止离开。对话框中的“保存并继续”复用同一保存结果，成功后继续原操作，失败后恢复对话框交互。

## 验证

源码契约测试覆盖保存期间不打开确认对话框、保存函数复用在途请求，以及“保存并继续”通过共享保存函数完成。执行前端全量测试、生产构建和差异检查。
