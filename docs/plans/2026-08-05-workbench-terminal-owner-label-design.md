# 工作台终态处理人标签设计

## 目标

消除工作台终态工作项中“当前处理人”的语义歧义。

## 方案

工作台继续展示既有的 `owner_id`，不调整后端字段或状态操作记录。在“已完成”和
“已终止”页签中将列表列名显示为“最后处理人”；待处理、未分派、异常中心和关注
视图仍显示“当前处理人”。

## 验证

增加前端源代码契约测试，断言列名根据当前页签动态计算，并运行前端测试和构建。
