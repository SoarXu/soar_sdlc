# 项目集重复名称提示中文化设计

## 目标

将项目集名称重复时返回的英文提示统一改为“项目集名称已存在”。

## 范围

- 保持现有的同级名称唯一性规则、HTTP 422 状态码和错误响应结构不变。
- 覆盖 MySQL 唯一索引预校验、通用回退校验和并发提交的唯一索引异常处理。
- 更新自动化测试，断言新的中文错误文案。

## 不在范围内

- 不调整项目集层级或名称规范化逻辑。
- 不修改其他英文错误提示。

## 验证

运行项目集名称唯一性测试，确认三条错误路径都返回 HTTP 422 和统一的中文文案。
