# 项目集操作栏间距设计

## 目标

项目集树中的项目集行和项目行从相同左边界开始，并让相邻操作按钮使用相同间距，同时保留各按钮的自然文本宽度。

## 方案

项目集列表把操作栏间距声明为 `6px` CSS 变量。共享工作流操作组件将外层动作组间距和主动作间距改为读取变量；未设置变量的页面继续使用当前的 `8px` 外层和 `4px` 主动作默认值。

## 验证

新增源码契约测试，确认项目集列表设置统一变量，工作流组件用变量控制两层 flex 间距。运行前端测试和生产构建。
