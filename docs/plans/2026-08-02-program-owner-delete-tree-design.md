# 项目集负责人删除树设计

## 目标

允许项目集负责人删除其治理范围内的非空项目集树，消除界面级联删除提示与后端“仅系统管理员可删非空树”的矛盾。

## 规则

- 项目集负责人和系统管理员均可删除其可治理的项目集树。
- 树中所有项目集必须为已关闭状态。
- 树中所有项目必须处于终态。
- 无治理权用户仍无删除权限；空项目集负责人仍可直接删除。

## 实现与验证

复用 `can_delete_program` 的树遍历和终态检查，仅移除非系统管理员的“必须为空”限制。新增服务层测试覆盖负责人删除已关闭且项目终态的非空树，以及仍被未关闭项目集或未终态项目拒绝的情况。
