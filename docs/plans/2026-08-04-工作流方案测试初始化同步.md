# 工作流方案测试初始化同步实施计划

**目标：** 使方案启用测试初始化完整的项目、需求、任务和 Bug 工作流，恢复失败测试提 Bug 链路的覆盖。

**范围：** `backend/tests/test_test_case_execution_api.py` 的测试准备数据。

## 步骤

1. 保留 `test_failed_case_bug_creation_succeeds_after_duplicate_bug_workflow_is_disabled` 的启用方案断言，并确认其当前只对需求、任务、Bug 应用模板。
2. 运行该单测，确认启用接口返回 `422`，原因是项目工作流未配置可用初始状态。
3. 将初始化对象类型补全为项目、需求、任务、Bug 四类；保持测试后续“新增重复 Bug 定义被拒绝，禁用后创建成功”的断言不变。
4. 重跑单测，验收它进入并覆盖失败测试用例创建 Bug 的完整链路。
5. 运行 `python -m pytest -q tests/test_test_case_execution_api.py` 与全量后端测试。
