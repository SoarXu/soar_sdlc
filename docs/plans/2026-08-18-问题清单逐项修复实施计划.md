# 问题清单逐项修复 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 逐项解决并验证 2026-08-18 后续问题清单的全部未解决条目。

**Architecture:** 以领域服务和 API 为约束唯一来源，视图层呈现同一规则并提供本地预校验。每项修复使用独立测试证明回归行为，迁移以幂等方式兼容历史数据。

**Tech Stack:** FastAPI、SQLAlchemy、MySQL、Vue 3、Element Plus、Node 契约测试、pytest。

---

### Task 1: 重新验证 N-001 至 N-004

**Files:**
- Verify: `backend/tests/test_requirement_real_iteration_assignment_api.py`
- Verify: `backend/tests/test_test_isolation.py`
- Verify: `backend/tests/test_test_case_scopes_api.py`
- Verify: `frontend/src/views/requirementRealIterationAssignment.test.mjs`
- Verify: `frontend/src/views/testCaseScopeList.test.mjs`
- Verify: `frontend/src/components/richTextPasteEditorPresentation.test.mjs`

1. 运行既有定向测试和构建。
2. 通过后将 N-001 至 N-004 标记为“已解决”，保留命令输出和验证日期。

### Task 2: N-005 列表操作一致性

**Files:**
- Modify: `frontend/src/views/TasksView.vue`
- Modify: `frontend/src/views/BugsView.vue`
- Test: `frontend/src/views/bugListActionColumnWidth.test.mjs`
- Create: `frontend/src/views/taskListActionColumnWidth.test.mjs`

1. 写出任务/缺陷操作列统一容器、按钮高度、对齐与最小点击区的失败契约。
2. 在两个列表应用共享操作列样式和确定性宽度。
3. 运行两个契约测试和前端构建。

### Task 3: N-006、N-007 迭代准入与流转门禁

**Files:**
- Modify: `backend/app/services/bug_service.py`
- Modify: `backend/app/services/workflow_runtime_service.py`
- Modify: `frontend/src/views/BugsView.vue`
- Test: `backend/tests/test_bug_workflow_api.py`
- Create: `backend/tests/test_bug_iteration_assignment_api.py`
- Test: `frontend/src/utils/bugIterations.test.mjs`

1. 先测试 Bug 无有效迭代时创建被拒绝，规划中/进行中迭代允许。
2. 先测试非进行中迭代的 Bug 不返回可执行流转。
3. 复用需求迭代资格服务，在后端实施约束，前端只显示可选迭代及对应提示。
4. 运行 API、视图契约测试。

### Task 4: N-008、N-009 选择性延期与审计

**Files:**
- Modify: `backend/app/services/iteration_service.py`
- Modify: `backend/app/views/iteration_view.py`
- Modify: `frontend/src/views/IterationDetailView.vue`
- Test: `backend/tests/test_iteration_detail_api.py`
- Create: `backend/tests/test_iteration_postpone_audit_api.py`
- Create: `frontend/src/views/iterationPostponeSelection.test.mjs`

1. 写出仅延期选中项、遗漏项保持原迭代、延期原因写入操作历史的失败测试。
2. 扩展完成迭代请求和服务，按工作项分组、选择项及原因创建审计记录。
3. 将弹窗改为纵向分组的复选列表，显示已选数量和延期原因。
4. 运行定向 API 与前端契约测试。

### Task 5: N-010、N-011 概览与工作台语义

**Files:**
- Modify: `backend/app/services/dashboard_service.py`
- Modify: `frontend/src/views/ProjectDetailView.vue`
- Modify: `frontend/src/views/DashboardView.vue`
- Test: `backend/tests/test_dashboard_workbench_api.py`
- Test: `frontend/src/utils/workbenchViewModel.test.mjs`
- Test: `frontend/src/views/projectPlanningWorkPool.test.mjs`

1. 写出未完成工作项计数、名称和当前处理人唯一展示/筛选的失败测试。
2. 在汇总和工作台模型中移除重复负责人字段，保留当前处理人。
3. 运行后端与前端定向测试。

### Task 6: N-012 工作流无处理人兜底

**Files:**
- Modify: `backend/app/services/workflow_runtime_service.py`
- Modify: `backend/app/services/workflow_definition_service.py`
- Modify: `frontend/src/components/WorkflowActionButtons.vue`
- Test: `backend/tests/test_workflow_runtime_api.py`
- Test: `frontend/src/components/workflowActionButtonsBehavior.test.mjs`

1. 先写项目负责人被允许角色授权时、无当前处理人仍可见并执行的失败测试。
2. 写出目标状态要求处理人但无候选人时阻止流转并提供说明的失败测试。
3. 收敛 `requires_owner` 语义并实现配置预检与执行兜底。
4. 运行工作流 API 和组件测试。

### Task 7: N-013 统一工作项评审

**Files:**
- Follow: `docs/plans/2026-08-18-手动与自动工作项评审实施计划.md`
- Modify: review services, runtime services, review views and `WorkflowActionButtons.vue` as specified therein
- Test: `backend/tests/test_work_item_review_api.py`
- Test: `backend/tests/test_git_triggered_work_item_review.py`
- Test: `frontend/src/views/gitTriggeredReviewWorkbench.test.mjs`

1. 按已确认计划先补齐手动提交、空 Diff、统一评审决定的失败测试。
2. 实现需求、任务、Bug 的统一服务端规则、弹窗与模板迁移。
3. 运行该计划规定的 API、迁移、前端测试。

### Task 8: N-014 流程图端点变更路由

**Files:**
- Modify: `frontend/src/components/WorkflowAdvancedConfigDrawer.vue`
- Modify: `frontend/src/components/WorkflowDesigner.vue`
- Modify: `frontend/src/utils/workflowManualRoute.js`
- Modify: `backend/app/services/workflow_definition_service.py`
- Test: `backend/tests/test_workflow_diagram_config_migration.py`
- Test: `frontend/src/components/workflowAdvancedConfigDrawer.test.mjs`
- Test: `frontend/src/utils/workflowManualRoute.test.mjs`

1. 先写出改目标状态后自动路由仍可保存、手工路由被明确要求重置的失败测试。
2. 变更端点时使自动配置失效并重新生成，手工配置提示重置；后端识别陈旧自动路径并返回可理解信息或重建。
3. 运行流程图定向测试。

### Task 9: 全量验证与问题清单

**Files:**
- Modify: `docs/issues/2026-08-18-后续问题清单.md`

1. 运行后端完整 `pytest`、前端全部 `*.test.mjs` 和 `npm run build`。
2. 逐项更新 N-001 至 N-014 状态为“已解决”，写入真实验证命令及结果。
3. 严格 UTF-8 验证问题清单和链接，运行 `git diff --check`。

