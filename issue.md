# 问题收集

## 待集中处理

### 1. 项目集列表操作按钮样式不一致

- 项目集列表中的项目行里，“启动”“更多”与“删除”按钮的尺寸、间距和视觉样式不一致。
- 处理时统一操作列按钮的尺寸、行高、间距和视觉规范。

### 2. 项目列表操作按钮样式不一致

- 项目列表中的生命周期动作按钮、“更多”与“删除”按钮的尺寸和视觉样式不一致。
- 处理时与项目集列表使用同一套操作列按钮规范。

### 3. 启动项目时错误要求填写代处理原因

- 现象：启动 `项目 2-update` 时，正常的“开始”动作要求填写“代处理原因”。
- 根因：工作流运行时将非项目 `owner_id`、但通过管理员或项目管理权限执行动作的用户识别为代处理者；通用代处理规则拒绝未携带 `delegate_reason` 的首次请求。
- 前端表现：收到 `Delegate reason is required` 后，工作流动作组件重新打开弹窗并将“代处理原因”设为必填。因此该字段不是正常项目启动表单的配置项。
- 处理方向：区分项目生命周期管理动作与工作项代处理。具备项目管理权限的用户启动项目不应填写代处理原因；代处理审计仅保留给替其他当前处理人处理工作项的场景。

### 4. 列表中直接展示项目暂停和关闭动作

- 项目集列表和项目列表的项目行中，“暂停”“关闭”当前被收进“更多”菜单。
- 在当前项目状态和用户权限允许时，将“暂停”“关闭”与“启动”一起直接展示在操作列。
- “更多”仅保留不需要即时可见的动作。

### 5. 明确默认工作流与分派规则方案的区别

- 现象：项目 `1-update`（ID `420`）未显示工作流方案，但其需求仍有工作流流转动作。
- 根因：`assignee_rule_config_id` 为空只代表未绑定自定义分派规则方案，不代表禁用工作流。
- 每个项目和工作项创建时都会初始化为已启用的系统默认工作流。项目 `420` 使用默认项目工作流，其需求使用默认需求工作流，因此需求流转动作符合预期。
- 处理方向：明确页面术语，例如空值显示“默认系统工作流”，或将列名改为“分派规则方案”，避免将未绑定自定义方案误解为没有工作流。

### 6. 工作台需要展示任务和 Bug

- 工作台需要展示任务和新创建的 Bug，与需求等其他工作项类型并列。
- 工作台入口、范围筛选、状态展示和可执行动作应遵循与其他工作项一致的权限和工作流规则。
- 当前遗漏：任务可以通过关联需求继承迭代归属，但工作台只检查任务自身的 `iteration_id`。例如项目 `420` 的任务 `2-task` 自身迭代为空，但关联需求 `128` 属于活动迭代 `61`，因此迭代详情正确展示任务，工作台却错误筛除它。
- 处理方向：工作台计算任务的有效迭代时，复用现有规则：优先使用任务自身迭代；未设置时使用关联需求的迭代。任务和 Bug 的活动迭代范围判断均应基于其有效迭代归属。

### 7. 工作流方案中重复的 Bug 定义导致提 Bug 失败

- 现象：从失败的测试用例执行记录创建 Bug 时，提示 `Project workflow scheme has no unique bug definition`。
- 根因：受影响项目绑定的工作流方案 `1` 中同时启用了两条 Bug 工作流定义：`debug-def` 和该方案的默认 Bug 工作流。创建 Bug 时要求 `bug` 对象类型只能匹配一条启用定义，因此无法决定使用哪一条。
- 处理方向：同一工作流方案内，每种对象类型只能保留一条启用的工作流定义；为已有重复定义提供管理员校验或修复入口；提 Bug 失败时返回可定位冲突定义的中文提示。
