# 项目操作按钮去重设计

## 目标

项目列表的每一行只显示一次“新增项目”，同时保留具备治理权限时的“编辑”和现有工作流、删除操作。

## 方案

`ProjectsView.vue` 的项目行在 `WorkflowActionButtons` 插槽中已经渲染了“编辑”和“新增项目”。插槽之后的第二个“新增项目”是重复渲染，应删除。

## 验证

新增源码级回归测试，断言 `WorkflowActionButtons` 的后置插槽只含一个“新增项目”按钮；运行前端测试和生产构建。
